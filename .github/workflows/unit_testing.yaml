name: Unit Testing

on:
  pull_request:
    branches:
      - main

jobs:
  unit_testing:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      # Add steps for setting up your unit tests and executing them here
